# ABOUTME: Project dependencies for HK Premier League Stats Platform (TFM/SaaS).
# ABOUTME: Aligned with GEMINI.md and project architectural principles.

# == Framework Principal ==
# Librerías centrales para la aplicación web y visualizaciones.
dash[testing]==4.0.0
dash-bootstrap-components==2.0.4
dash-cytoscape>=1.0.0
plotly==6.6.0
Flask==3.0.3
werkzeug==3.0.3

# == Persistencia de Datos (SQLAlchemy) ==
# Para el nuevo motor de base de datos (SQLite/PostgreSQL).
SQLAlchemy==2.0.29
psycopg2-binary==2.9.9 # Driver para PostgreSQL (Supabase)

# == Autenticación y Sesiones ==
# Manejo de logins y sesiones de usuario.
Flask-Login==0.6.3

# == Procesamiento y Análisis de Datos ==
# Manipulación de datos y cálculos numéricos.
pandas==2.2.2
numpy==1.26.4
pyarrow>=15.0  # Parquet serialization for processed DataFrame cache

# == Extracción de Datos (Web Scraping) ==
# Para obtener datos de fuentes web.
requests==2.31.0
beautifulsoup4==4.12.2
lxml==5.2.2  # Parser de alto rendimiento para BeautifulSoup

# == Generación de Documentos ==
# Creación de reportes en formato PDF.
reportlab==4.4.1
pillow>=10.0

# == Utilidades de Desarrollo ==
# Herramientas para caché, variables de entorno y logging.
python-dotenv==1.0.0
Flask-Caching==2.1.0
colorlog==6.7.0  # Para logs con colores en la consola
icalendar==6.1.1  # Para parsear calendarios ICS de la HKFA
joblib>=1.3

# == Inteligencia Artificial & ML (Track 1, 2 & 3) ==
# Stack para el TFM según .gemini/rules/ai-ml.md
xgboost>=2.0
tabpfn>=0.1.7,<0.2 # Note: Large model (~1GB download)
shap>=0.45
scikit-learn>=1.4
umap-learn>=0.5.3
sentence-transformers>=2.7
rembg>=2.0
google-genai>=0.3.0
langgraph>=0.2.2
langchain-google-genai>=2.0.1
langchain-google-vertexai>=1.0

# == Servidor de Producción ==
# Servidor WSGI para despliegue en entornos de producción.
gunicorn==21.2.0

# == Testing ==
# Frameworks y herramientas de pruebas unitarias e integración.
pytest==8.2.2
pytest-cov==5.0.0
requests-mock==1.12.1
playwright==1.58.0
playwright-stealth==2.0.3
googlesearch-python
